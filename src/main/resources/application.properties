spring.application.name=groupware
spring.jpa.hibernate.ddl-auto=none

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

spring.datasource.url=jdbc:mysql://localhost:3306/groupware_db?useUnicode=true&characterEncoding=utf8

spring.datasource.username=gw_user
spring.datasource.password=${DB_PASSWORD}

# MyBatis CamelCase ?? (DOC_ID -> docId ?? ??)
mybatis.configuration.map-underscore-to-camel-case=true

# Model ????? ?? ??? ?? (Mapper XML?? ?? ?? ?? ??)
mybatis.type-aliases-package=com.byh.groupware.domain.approval.model, \
                             com.byh.groupware.domain.dept.model, \
                             com.byh.groupware.domain.user.model, \
                             com.byh.groupware.domain.company.model¸ \
                             com.byh.groupware.domain.approval.dto

# Mapper XML ?? ?? ?? (src/main/resources/mapper ?? ??)
mybatis.mapper-locations=classpath:mapper/**/*.xml

# (??) ?? ?? ???
logging.level.com.byh.groupware.approval.mapper=debug